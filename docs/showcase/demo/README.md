# Showcase demo

A dummy project used for keplr's README screenshots — nothing here is real,
it just exercises the editor, search, tasks, terminal, and git panel.

- `main.rs` — Rust with tree-sitter highlighting
- `server.py` — Python API sketch
- `keplr.json` — one task so the Tasks panel has something to run
